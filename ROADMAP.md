# Zenith — план разработки

Составлен 23 сентября 2026 по итогам ресёрча рынка и спора агентов
([отчёт «Горизонт Zenith»](https://claude.ai/artifact/5FEPKSPUtfXx8yLL6WxfC8)) и решения автора,
что оставить. Файл рассчитан на то, чтобы новый сеанс прочитал его и начал с нужного этапа.

**Как пользоваться.** Этапы идут в порядке зависимостей: этап 0 — фундамент, на который
опирается всё остальное. Этапы 2 и 3 независимы и могут идти параллельно. Отмечайте
`[x]` по мере готовности. Одна ветка и один PR на этап (как было с `audit-*`).

---

## Правила для каждой задачи (Definition of Done)

Каждая функция из этого плана считается готовой, только когда:

- [ ] **Зарегистрирована в реестре функций** (этап 0.2): свой `featureId`, модуль,
      значение по умолчанию для каждого шаблона-профиля. Выключенная функция не рисует
      интерфейс, не пишет в файлы, не ходит в сеть и не ставит таймеры.
- [ ] **Настройки** описаны в схеме модуля (`settings.schema.ts` / `src/settings/schema`)
      и в `src/modules/sync/statePolicy.ts` (там каждый ключ обязателен — иначе ошибка сборки).
      Секреты помечены так, чтобы никогда не попадать в экспорт профиля.
- [ ] **Строки** на двух языках (en, ru).
- [ ] **Документация** обновлена в `docs/en/*.md` и `docs/ru/*.md`; если есть сеть —
      раздел в `privacy.md`.
- [ ] **Данные** — обычный Markdown/frontmatter; ничего не переписывается молча;
      «не записано» ≠ «провал».
- [ ] **Телефон**: у всего, что работает на наведении или перетаскивании, есть касательная
      альтернатива.
- [ ] **Тесты** vitest на чистую логику; `npm run typecheck && npm run lint && npm test`.
- [ ] Комментарии в коде объясняют «почему» — как во всём остальном репозитории.

## Перед стартом

- [x] Закоммитить текущую незавершённую работу отдельной веткой: сейчас ~60 изменённых
      файлов (новые `src/components/ui/fields/`, `QuickAddForm.tsx`, галереи в debug,
      правки модалок и стилей). Новые этапы не должны с ней смешиваться.
      *(Влито в PR #20 до начала этапа 0.)*
- [x] Открытые вопросы закрыты — см. [решения автора](#решения-автора-23-сентября-2026).
- [x] Время намаза: причина найдена — настройки метода, не баг (см. этап 3).

---

## Этап 0. Фундамент

### 0.1 Строка задачи без потерь — баг, потеря данных

Почти каждая новая функция пишет строки задач (ввод естественным языком, перетаскивание
по сетке, ритуалы, напоминания «выполнено»). Сейчас это опасно:

- `➕ дата` (дата создания из плагина Tasks) стирается при любой правке:
  `ALL_MARKERS_RE` в `src/modules/tasks/services/taskFormat.ts` вырезает её из заголовка,
  поля для неё нет, а `updateLineExtras` / `updateTaskInFile` в `taskWriter.ts` собирают
  строку заново.
- `🔁 every week 🏁 delete` не продлевается: `RECUR_RE` не останавливается на 🆔, ⛔, 🏁.
- Приоритеты 🔺 и ⏬ не распознаются.

Задачи:
- [x] Одна таблица маркеров, из которой строятся все регулярки (сейчас набор символов
      переписан вручную в двух местах — отсюда баг с `RECUR_RE`).
- [x] Неизвестные маркеры (➕ 🆔 ⛔ 🏁 и любые другие) сохраняются в исходном порядке.
- [x] Правка накладывает изменённые поля на разобранную строку, а не собирает её с нуля.
- [x] 🔺/⏬ читаются как крайние уровни для сортировки, значок на диске остаётся, пока
      пользователь сам не сменит приоритет. Тип `Priority` не расширять.
- [x] Новое повторение не копирует 🆔; `🏁 delete` удаляет выполненную строку.
- [x] Тесты (`tests/taskFormat.test.ts`, `tests/taskWriterGuards.test.ts`): строки из
      документации Tasks проходят разбор и сборку байт в байт; правка заголовка сохраняет
      ➕ и 🆔; повтор с 🏁 продлевается; 🔺 переживает правку.

> Сделано в ветке `foundation`. Попутно: перенос задачи в корзину больше не стирает ⏰/⏱/⏲/✅,
> смена статуса в окне правки теперь сохраняется, переименование подзадачи в списке не
> затирает её маркеры, теги читаются по правилам Obsidian (`#работа` — тег, `C#` и `#123` — нет),
> новое повторение не копирует также ⛔ и ссылку на блок (как плагин Tasks), `➕` ставится на сегодня.

### 0.2 Реестр функций (feature flags)

Главная концепция плагина — можно выключить всё, чем не пользуешься. Нужна одна точка правды.

- [ ] `src/core/features.ts`: реестр `FeatureDefinition { id, moduleId, labelKey, descKey,
      default, requires?: FeatureId[], settingKey?: keyof ZenithSettings }`.
      `id` в виде `модуль.функция`: `tasks.naturalInput`, `calendar.dragSchedule`…
- [ ] Хранение: `settings.features: Record<FeatureId, boolean>` (scope `shared`).
      Уже существующие тумблеры (`weatherShowAir`, `prayerShowSunrise`,
      `journalCaptureTasks`, `dashboardShowDate`…) **не переименовывать** — регистрировать
      в реестре со ссылкой `settingKey`, чтобы профили управляли ими так же.
- [ ] API: `isFeatureOn(id)` для кода вне React, `useFeature(id)` — хук с подпиской.
      `requires` учитывается: функция без включённой зависимости считается выключенной,
      а в настройках показано почему.
- [ ] В форме настроек каждого модуля — группа «Функции» с тумблерами из реестра, с
      описанием и пометкой зависимостей. Генерируется из реестра, а не вручную.
- [ ] **Аудит существующих функций**: пройти каждый модуль и завести флаг на каждую видимую
      функцию. Черновой список:
  - Dashboard: заголовок/приветствие, дата, фон, прозрачность карточек, пресеты раскладки.
  - Tasks: панель статистики, heatmap, таймер, вложения, подзадачи, перетаскивание,
    умные корзины, группы по файлам, захват в дневную заметку.
  - Calendar: просроченные, ленты 🛫→📅, дневные заметки, все 24 часа, недельный/дневной
    вид, агенда, виджет, подсветка при наведении.
  - Projects: виджет, связка задач по ссылке.
  - Journal: блок `zenith-daily`, цвет календаря по настроению, месяц привычек,
    статистика, счётчик слов, шаблон.
  - Content: импорт, статистика, полка «продолжить», кнопка «+1», мультивыбор,
    фильтр по жанрам.
  - Prayer: восход, тахаджуд и доп. времена, напоминания, хиджра, статистика,
    полоса недели, виджет.
  - Weather: качество воздуха, почасовой, солнце, дни прогноза.
  - Прочее: иконки папок, заготовка хранилища, медиа-баннер, картинка, навигатор.
- [ ] Миграция (`settingsVersion`): у существующих пользователей всё, что работает
      сейчас, остаётся включённым. Автоматически сохранить снимок «До профилей».
- [ ] Тесты: `requires`, миграция, чтение через `settingKey`.

### 0.3 Центр уведомлений и общий планировщик (core)

Нужен напоминаниям о задачах, намазу, ритуалам и отсчётам.

- [ ] `src/core/scheduler.ts`: вынести из `PrayerReminderService` таймер «следующее событие
      после сейчас» и `STALE_MS`. Источники регистрируют функцию `nextEvents(now)`.
- [ ] `src/core/notifications/`: центр уведомлений.
  - Запись: `{ id, source, title, body, at, actions[], readAt?, snoozedUntil? }`.
  - Хранится **на каждом устройстве своя** (решение автора; scope `device`), история
    30 дней, лимит записей.
  - **Ничего не теряется**: при запуске Obsidian события, чьё время прошло, пока приложение
    было закрыто, попадают в центр как «пропущенные», а не молча исчезают.
  - Каналы доставки: уведомление Obsidian (`Notice`); системное уведомление на компьютере
    (Web Notification API в Electron) — по желанию; на телефоне только внутри приложения,
    и в настройках об этом честно сказано.
  - «Не беспокоить» по часам; тумблер на каждый источник.
- [ ] Интерфейс: колокольчик с числом непрочитанных (в шапке дашборда и командой),
      список «сегодня / раньше», действия: открыть, отложить (10 мин / 1 ч / завтра),
      отметить выполненным, убрать.
- [ ] Перевести напоминания о намазе на центр (без изменения поведения по умолчанию).

### 0.4 Помощники периода и темпа

- [ ] `src/core/period.ts`: `periodProgress(start, end, today)` и
      `pace(target, done, start, end, today)` → ожидаемое к сегодня, отставание/опережение,
      нужный темп в день. ~60 строк с тестами. Общей сущности «Цель» не заводить.

---

## Этап 1. Профили настроек

Цель: после установки человек выбирает шаблон за минуту, а не проводит час в настройках.

- [ ] **Встроенные шаблоны** (набор модулей + флаги функций + несколько настроек):
  - «Минимум» — дашборд, задачи, дневник без дополнений.
  - «Всё» — всё включено.
  - «Привычки и дневник» — дневник, цели, «бросить», год в пикселях, вопрос дня, ритуалы.
  - «Задачи и планирование» — задачи, календарь, проекты, ввод текстом, напоминания,
    перетаскивание, ритуалы, отсчёты.
  - «Намаз и пост» — намаз, пост, ифтар/сухур, напоминания о намазе, дневник в минимуме
    (намазы пишутся в дневную заметку — это зависимость в реестре).
  - «Чтение и медиа» — контент, челлендж, дневник повторов.
- [ ] **Применение**: предпросмотр разницы («будет выключено: …, включено: …»), два режима —
      «заменить» и «добавить к текущему». Кнопка «Вернуть как было» (снимок до применения).
- [ ] **Свои профили**: сохранить текущее состояние под именем (`settings.profiles`,
      scope `shared`), переименовать, удалить, применить.
- [ ] **Экспорт/импорт** в JSON: в файл хранилища (`Zenith/profiles/<имя>.json`) и через
      буфер обмена (так работает и на телефоне).

  ```json
  {
    "zenith": "profile",
    "version": 1,
    "name": "Мой минимум",
    "createdAt": "2026-09-23",
    "modules": ["dashboard", "tasks", "journal"],
    "features": { "tasks.naturalInput": true, "tasks.reminders": false },
    "settings": { "uiDensity": "compact", "journalTrackers": [] }
  }
  ```

  - Что попадает: ключи со scope `shared` плюс безопасные `device`-настройки интерфейса.
    **Никогда**: пароли, токены, ключи S3, client id, пароль шифрования.
    Пути к папкам — только по галочке «включить пути» (у другого человека другое хранилище).
  - Импорт: проверка версии и типов по значениям по умолчанию из схем, неизвестные ключи
    и флаги отбрасываются с отчётом, затем тот же предпросмотр разницы.
- [ ] **Первый запуск**: окно с карточками шаблонов. У существующих пользователей окно не
      показывается (их состояние уже сохранено как «До профилей»).
- [ ] Страница настроек «Профили»; документация en/ru.

---

## Этап 2. Контент без внешних API

Решение автора: автозаполнение работает не так, как задумано, — убрать полностью и оставить
данные за пользователем.

- [ ] Удалить (~1400 строк): `src/modules/content/services/metadata/` (index, providers:
      anilist, animanga, books, itunes, jikan, steam, wikipedia; resync, types),
      `components/MetadataPicker.tsx`, `components/useMetadataSearch.ts`,
      `services/coverCache.ts`.
- [ ] Убрать поиск и автозаполнение из `ContentForm.tsx`, «Обновить метаданные» из
      `ContentDetailModal.tsx`; проверить `contentWriter.ts`, `contentDuplicates.ts`,
      `contentImport.ts` на ссылки на `sourceId`/провайдеров.
- [ ] Настройку `cacheCovers` убрать из `settingsSlice`, `statePolicy`, схемы; миграция
      удаляет ключ.
- [ ] Оставить: импорт из файлов MAL / Goodreads / Letterboxd (это разбор файла, не API).
- [ ] Обложка — **оба варианта** (решение автора): файл хранилища (`VaultImageField`) или
      ссылка, которую пользователь вставил сам (картинка грузится при показе, без
      скачивания в хранилище). В `privacy.md` — одной строкой.
- [ ] Поля `externalRating`, `source`, `sourceId` — **поддержку убрать полностью** (решение
      автора): из типов, парсера, записи, карточки, статистики, поиска дубликатов.
      Сами строки в старых заметках не вычищать — плагин просто перестаёт их читать;
      запись элемента должна сохранять незнакомые ключи frontmatter как есть.
- [ ] Удалить неиспользуемые строки i18n и тесты провайдеров; обновить `content.md` и
      `privacy.md` (контент больше не ходит в сеть).
- [ ] Возврат автозаполнения — только сторонним модулем через точку расширения
      `content.metadataProvider` (этап 9).

---

## Этап 3. Время намаза: годовая таблица офлайн

Что уже есть: источник по умолчанию — Aladhan (`src/modules/prayer/prayerApi.ts`), месяц за
запрос, запасной вариант — расчёт на устройстве (`prayerSource.ts`, origin
`local | api | fallback`).

**Диагноз поставлен (23.09.2026).** Время расходилось с приложением IslamApp, стабильно.
Сравнение на Ставрополе (45,03° с. ш.) за 23.09:

| | Фаджр | Зухр | Аср | Магриб | Иша |
|---|---|---|---|---|---|
| Zenith (ДУМ РФ 16°/15°, ханафитский аср) | 04:33 | 12:05 | 16:18 | 18:08 | 19:29 |
| IslamApp | 04:21 | 12:04 | 15:27 | 18:08 | 19:41 |
| Движок Zenith: MWL 18°/17°, стандартный аср | 04:21 | 12:05 | 15:27 | 18:08 | 19:41 |

Прогон движка плагина по всем методам: **MWL + стандартный (шафиитский) аср совпадает с
IslamApp до минуты**, кроме зухра (+1 — округление: плагин округляет полдень до ближайшей
минуты, приложение, похоже, отбрасывает секунды). Движок верен; расхождение давали настройки
по умолчанию (`russia` + `hanafi`), а не баг. Подбор углов перебором дал ровно 18°/17°.
IslamApp, по словам автора, сам выбирает мазхаб для асра по местоположению.

Выводы для этапа:
- Функция «Подобрать под моё приложение» доказала смысл — ровно этот подбор, только
  автоматический.
- Метод и мазхаб нельзя молча ставить по умолчанию: при первом включении модуля
  (и в шаблоне «Намаз и пост») спрашивать явно, со ссылкой на подбор.
- Выбор мазхаба — религиозное решение пользователя; плагин предлагает, но не решает.

Слабые места кеша всё равно есть и чинятся на шаге 3: кеш в `localStorage` (свой на каждом
устройстве, может очищаться), месяц грузится лениво (первый кадр — по расчёту), дни
соседнего месяца и офлайн показывают расчёт.

Шаги:
- [ ] **1. «Подобрать под моё приложение»** — главный шаг этапа. В настройках намаза
      пользователь вводит сегодняшние 6 времён из своего приложения. Плагин перебирает
      методы × мазхаб × правило высоких широт (локально и, если включено, через провайдер),
      находит вариант с наименьшей ошибкой и предлагает его вместе с поправками по каждому
      намазу (`prayerAdjustments` уже есть). Показывает остаточную разницу по каждому
      намазу. Хорошо бы проверить на двух-трёх днях в разные сезоны — ихтият и высокие
      широты проявляются по-разному зимой и летом.
- [ ] Округление: настройка «округлять время: до ближайшей минуты / отбрасывать секунды»
      (так совпадёт и зухр без ручной поправки).
- [ ] Явный выбор метода и мазхаба при первом включении модуля намаза.
- [ ] В виде намаза — пометка источника, когда показан расчёт вместо таблицы
      («таблица ещё не загружена»).
- [ ] **2. Провайдеры.** Интерфейс
      `PrayerProvider { id, label, fetchYear(place, year, opts): Promise<YearTable | null> }`.
  - **Aladhan** (по умолчанию): годовой календарь одним запросом
    `GET https://api.aladhan.com/v1/calendar/{year}?latitude=&longitude=&method=&school=&latitudeAdjustmentMethod=&midnightMode=&timezonestring=`.
    Метод 14 — «Spiritual Administration of Muslims of Russia» (16°/15°).
    Внимание: `methodSettings` (свои углы, метод 99), который сейчас использует код,
    отсутствует в текущей OpenAPI-спецификации — проверить, работает ли; если нет, свои
    углы доступны только в расчёте на устройстве.
  - **Muftyat.kz** — официальный API ДУМ Казахстана, год за запрос:
    `https://api.muftyat.kz/prayer-times/{year}/{lat}/{lng}` (города —
    `https://api.muftyat.kz/cities/`). Покрывает Казахстан; условия использования на сайте
    не указаны — уточнить перед включением.
  - **Своя таблица** (низкий приоритет — у автора печатного расписания нет) — CSV или
    таблица Markdown в хранилище (`date,fajr,sunrise,dhuhr,asr,maghrib,isha`). Офлайн и
    точно «как в мечети» для тех, у кого расписание есть.
  - **Расчёт на устройстве** — альтернатива в настройках, как и просил автор.
  - UmmahAPI рассмотрен и не выбран: годового эндпоинта не нашлось, данные о надёжности —
    только из блога самого сервиса.
- [ ] **3. Офлайн.** Таблица года хранится файлом в папке плагина
      (`.obsidian/plugins/zenith/cache/prayer/<провайдер>-<место>-<метод>-<год>.json`),
      а не в `localStorage`. Загрузка текущего года — при смене места/метода/провайдера;
      следующего — когда до конца года меньше 60 дней. Повторы с увеличивающейся паузой,
      отрисовку никогда не блокировать. Однократный перенос старого кеша из `localStorage`.
- [ ] **4. Запасной вариант виден**: настройка «если таблицы нет: показывать расчёт /
      показывать прочерк».
- [ ] **5. Приватность**: координаты перед отправкой округлять до 0,01° (~1 км);
      `privacy.md` называет, какой сервис получает приблизительное место.
- [ ] Тесты: разбор ответов каждого провайдера на фикстурах, ключ кеша, порядок источников,
      переход на летнее время (`timezonestring` — зона устройства).

---

## Этап 4. Задачи и календарь

### 4.1 Ввод естественным языком — `tasks.naturalInput`
- [ ] Чистый парсер `src/modules/tasks/services/quickParse.ts` (RU + EN), общий с URI (4.2).
- [ ] Грамматика (узкая, всё остальное остаётся текстом):
  - даты → 📅: `сегодня/today`, `завтра/tomorrow`, `послезавтра`, `пн…вс / mon…sun`
    (с «в» или без), `+3д / +3d`, `+2н / +2w`, `15.10`;
  - время → ⏰: `в 18`, `18:00`, `18:30`, `18–19`;
  - приоритет: `!!` → ⏫, `!` → 🔼;
  - повтор → 🔁 только то, что понимает движок: `каждый день/неделю/месяц/год`,
    `каждые N дней/недель`;
  - `#тег` — родной формат, нужно только автодополнение.
- [ ] В `QuickAddForm.tsx` под полем — чип на каждый распознанный кусок, каждый можно убрать.
      Нераспознанное не получает чип — видно, что фраза не понята, ничего не теряется молча.
- [ ] Табличные тесты на обе грамматики.

### 4.2 Захват задач по URI — `tasks.uriCapture` (по умолчанию выключено)
- [ ] `registerObsidianProtocolHandler('zenith', …)` (сейчас зарегистрирован только
      `zenith-dropbox`). Действия: `add-task` (через `quickParse`), `log`
      (`tracker=water&value=+1`), `open` (вид).
- [ ] Безопасность: любая веб-страница может открыть `obsidian://`, поэтому — выключено по
      умолчанию, только добавление, каждая запись — уведомление с отменой, ограничение
      частоты, проверка, что модуль включён.
- [ ] Документация с примерами для «Быстрых команд» iOS и Android.

### 4.3 Напоминания о задачах — `tasks.reminders` (на центре из 0.3)
- [ ] Правила: в момент ⏰; общий сдвиг «за N минут»; утренняя сводка для задач на сегодня
      без времени («5 задач, 2 просрочены»).
- [ ] Из уведомления: открыть, отложить, отметить выполненной (через `taskWriter`).
- [ ] Если включён плагин Reminder (`obsidian-reminder`), предупредить о двойных
      напоминаниях.

### 4.4 Перетаскивание по часовой сетке — `calendar.dragSchedule`
- [ ] Перенос блока в пределах дня меняет начало ⏰; растягивание — конец диапазона ⏰
      (не ⏲). Перенос на другой день меняет 📅 — с подтверждением «это срок».
- [ ] Колонка «без времени» с задачами дня: бросил на сетку — поставилось ⏰.
- [ ] Телефон: касание пустого слота → выбор задачи; клавиатура — стрелки у сфокусированного
      блока.
- [ ] Использовать существующие механизмы (`useGridDrag`, `useSortableRows`) и запись через
      `updateLineExtras` (после 0.1). Подсветка при наведении (`useSpotlight`) уже сделана.

---

## Этап 5. Дневник и привычки

### 5.1 Цели «N раз в неделю», пределы и дни отдыха — `journal.goals`
- [ ] Расширить `JournalTracker` (`src/core/journalConfig.ts`), обратно совместимо:
      `goalPeriod?: 'day' | 'week'`, `goalCount?: number`,
      `goalDirection?: 'atLeast' | 'atMost'`.
- [ ] День отдыха — значение во frontmatter, которое пишет сам пользователь
      (`sport: rest`); серию не рвёт и не продлевает.
- [ ] Недельная квота как второй вид серии в `trackerHistory` / `habitMonth` /
      `journalStats`; «не больше» — ветка в `meetsGoal`. Смена цели не пересчитывает прошлое
      молча.
- [ ] Редактор трекера в `JournalSettings.tsx`.

### 5.2 Привычки «бросить» — `journal.quitHabits`
- [ ] Режим трекера `quit` = предел «не больше 0» по **записанным** дням.
- [ ] Подпись честная: «12 из 15 записанных дней без X», а не «15 дней без X».

### 5.3 Год в пикселях — `journal.yearPixels`
- [ ] Годовой масштаб `JournalCalendar`: 365 клеток по выбранной шкале; незаписанные дни
      нейтральные, а не «плохие»; клик — день.

### 5.4 Вопрос дня — `journal.dailyPrompt`
- [ ] Источник: заметка пользователя (строка = вопрос) или небольшой встроенный
      двуязычный набор нейтральных вопросов. Вопрос один на весь день.
- [ ] Показ в блоке `zenith-daily` (тумблер) и подстановка `{{prompt}}` в шаблоне.
      Вставка в заметку — по клику. Никаких серий.

### 5.5 Ритуалы утра и вечера — `journal.rituals`
- [ ] Команда, карточка на дашборде и (по желанию) уведомление через центр: по умолчанию
      **08:00 и 21:00, напоминания выключены**.
- [ ] Утро: задачи на сегодня, выбрать главную, просмотр просроченных.
- [ ] Вечер: сделанное за день; незакрытые задачи сегодняшнего и прошлых дней →
      «сегодня / отложить / отменить». Перенос меняет ⏳ **в самой строке**, строка не
      переезжает (обещание из `journal.md`). Строка «на завтра» — в дневную заметку.
- [ ] Каждый шаг можно пропустить и отключить в настройках. Никаких серий за ритуал.

---

## Этап 6. Контент и намаз

### 6.1 Годовой челлендж — `content.challenge`
- [ ] Цель на год для каждого типа («24 книги»); прогресс и темп через `pace()` из 0.4.
- [ ] Виджет и шапка статистики контента. Опция «считать повторы» (связь с 6.2).

### 6.2 Дневник повторов — `content.readings`
- [ ] Модель: `readings: ["2019-03-01/2019-03-20", "2026-08-02/"]` — все прочтения/просмотры
      строками ISO-интервалов (последний может быть открыт). `started` (первое начало) и
      `finished` (последнее окончание) остаются зеркалами — 11 файлов, которые их читают,
      не трогаем.
- [ ] Перезапуск завершённого элемента: если `readings` нет — создать из `started/finished`,
      затем добавить `сегодня/`. Завершение закрывает открытый интервал и ставит `finished`.
- [ ] **Исправить баг статистики**: «среднее время прочтения» (`contentStats.ts:111`) сейчас
      считает от первого `started` до последнего `finished` — перечитанная через семь лет
      книга выглядит как семилетнее чтение. Считать по интервалам `readings`.
- [ ] Статистика «перечитано N», история в карточке элемента.

### 6.3 Трекер поста — `prayer.fasting`
- [ ] Свойство дневной заметки `fast: ramadan | qada | nafl | broken | excused`.
      День Рамадана без записи — не пропуск.
- [ ] Прогресс Рамадана («день 12 из 30, постился 11»), счётчик восполненных постов.
- [ ] Подсказки добровольных постов (пн/чт, «белые дни» 13–15 по хиджре, Ашура, Арафа) —
      отдельный тумблер. Хиджра и Рамадан уже есть в `hijri.ts`.

### 6.4 Ифтар и сухур в обратном отсчёте — `prayer.iftarSuhoor`
- [ ] В Рамадан и в дни с `fast:` отсчёт до магриба подписан «до ифтара», до фаджра —
      «сухур заканчивается через…».
- [ ] Отступ имсака — настройка, по умолчанию 0, с подписью.

---

## Этап 7. Дашборд

### 7.1 Обратные отсчёты и прогресс периода — `dashboard.countdowns`, `dashboard.periodProgress`, `dashboard.lifeWeeks`
- [ ] Прогресс периода: день / неделя / месяц / год (+ месяц хиджры и Рамадан — опция).
- [ ] Отсчёты: из существующих дат (📅 задач с отметкой, `targetDate` проектов, события
      хиджры — «до Рамадана 12 дней», Ид) и свои события (в настройках виджета).
- [ ] «Жизнь в неделях»: нужна дата рождения и ожидаемая продолжительность — отдельный
      тумблер, **включается только вручную**, ни один шаблон (включая «Всё») его не включает.
- [ ] Всё на `periodProgress()` из 0.4.

### 7.2 Дашборд как стартовая страница — `dashboard.openOnStartup` (по умолчанию **включено**)
- [ ] После `onLayoutReady` открыть дашборд; если вкладка уже восстановлена — только
      показать её, не плодить копии.
- [ ] Если включён плагин Homepage (`homepage`) — вместо настройки подсказка о конфликте.
- [ ] Телефон: сначала каркас, данные потом — не удлинять и без того долгий запуск.

---

## Этап 8. Обзоры и итоги

### 8.1 Периодические обзоры — `journal.reviews`
- [ ] Блок `zenith-summary` с `period=week|month|quarter|year` и необязательной `date=`
      (по умолчанию — из имени/свойств заметки, как делает `zenith-daily`). Фиксированный
      набор разделов (`show=tasks,habits,content,prayer,fasting,words`), без языка запросов.
      Считается живьём; кнопка «Зафиксировать» дописывает снимок в Markdown ниже.
- [ ] Заметки недели/месяца/квартала/года: папки, шаблоны имён и шаблоны содержимого в
      настройках; команды «Открыть заметку недели/месяца/года» — создают по клику,
      никогда сами. Флаг `reviewed: true`.
- [ ] Номер недели в календаре дневника → заметка недели.
- [ ] Если включён Periodic Notes (`periodic-notes`) — предупредить о двух комплектах
      заметок, как уже сделано с core Daily notes.

### 8.2 Итоги года — `journal.yearInReview`
- [ ] Вид «Итоги года» на данных `zenith-summary period=year`: разделы по включённым
      модулям — задачи и теги, лучшие серии привычек, настроение по месяцам (год в пикселях),
      слова в дневнике, прочитанное/просмотренное и челлендж, намазы вовремя, пост.
- [ ] Доступен с 1 декабря и по запросу за любой прошлый год.
- [ ] Картинка для публикации — по желанию; разделы про намаз и здоровье в ней выключены
      по умолчанию.

---

## Этап 9. API модулей v2: сторонние модули меняют встроенные

Сейчас (`src/core/moduleApi.ts`, v1) модуль может только добавлять своё: виджеты, кнопки
навигатора, виды, команды, блоки кода, иконки, переводы. Точек расширения встроенных модулей
нет.

### 9.1 Разрешения
- [ ] В манифесте — `permissions`: `tasks:read`, `tasks:write`, `journal:read`,
      `journal:write`, `content:read`, `content:write`, `calendar:layers`, `ui:slots`,
      `settings:<модуль>`, `network:<хост>`…
- [ ] Окно согласия перечисляет их простыми словами; изменение списка при обновлении —
      повторное согласие.
- [ ] API-фасад отдаёт только разрешённые пространства; вызов без разрешения — понятная
      ошибка.

### 9.2 Точки расширения
- [ ] `tasks`: свои маркеры/поля (работают благодаря 0.1), действия в меню задачи,
      фильтры и сортировки.
- [ ] `journal`: свои виды трекеров, разделы панели дня.
- [ ] `calendar`: слои событий только для чтения — сюда ляжет отложенное чтение ICS как
      сторонний модуль.
- [ ] `content`: `metadataProvider` — вернуть автозаполнение можно будет только так, со
      своим разрешением `network:`.
- [ ] `prayer`: `registerProvider` — провайдер своего муфтията.
- [ ] `ui`: именованные слоты во встроенных видах (`tasks.item.afterTitle`,
      `journal.day.afterTrackers`…) и заменяемые отрисовщики с откатом на встроенный при
      ошибке. Никакого патчинга внутренностей.
- [ ] `settings`: добавить поля в страницу встроенного модуля; `features.register` —
      функции стороннего модуля попадают в профили.
- [ ] События: `task:completed`, `task:created`, `journal:recorded`… (только чтение).
- [ ] Запись во встроенные данные — только через существующие писатели с их защитами
      (`taskWriterGuards`), не напрямую в файлы.

### 9.3 Безопасность
- [ ] Хеш кода: заменить 32-битный FNV-1a (`moduleInstaller.ts`) на SHA-256 (SubtleCrypto) —
      FNV легко подобрать. Можно сделать раньше, отдельным маленьким PR.
- [ ] Журнал действий модуля (что и куда записал) в настройках.
- [ ] Граница ошибок вокруг каждого расширения; автоотключение после повторных сбоев.
- [ ] «Безопасный режим» — запуск без сторонних модулей (команда и настройка).
- [ ] Честно в документации: `new Function` — изоляция, а не песочница
      (`moduleEval.ts` уже это признаёт). Разрешения — договор и помощь при проверке
      кода; настоящая защита — согласие, закреплённый хеш и читаемый исходник.
- [ ] `ZENITH_MODULE_API_VERSION = 2`, модули v1 продолжают работать.
- [ ] Пример-модуль в `modules_def/`, который использует слот и точку расширения.
- [ ] `docs/en/module-api.md` и `docs/ru/module-api.md`.

---

## Карта: 17 выбранных функций

| # | Функция | Этап | Флаг |
|---|---|---|---|
| 1 | Ввод естественным языком | 4.1 | `tasks.naturalInput` |
| 2 | Напоминания о задачах + центр уведомлений | 0.3, 4.3 | `notify.center`, `tasks.reminders` |
| 3 | Перетаскивание по часовой сетке | 4.4 | `calendar.dragSchedule` |
| 4 | Цели «N раз в неделю», пределы, дни отдыха | 5.1 | `journal.goals` |
| 5 | Привычки «бросить» | 5.2 | `journal.quitHabits` |
| 6 | Год в пикселях | 5.3 | `journal.yearPixels` |
| 7 | Ритуалы утра и вечера | 5.5 | `journal.rituals` |
| 8 | Периодические обзоры | 8.1 | `journal.reviews` |
| 9 | Вопрос дня | 5.4 | `journal.dailyPrompt` |
| 10 | Годовой челлендж | 6.1 | `content.challenge` |
| 11 | Дневник повторов | 6.2 | `content.readings` |
| 12 | Трекер поста | 6.3 | `prayer.fasting` |
| 13 | Ифтар и сухур в отсчёте | 6.4 | `prayer.iftarSuhoor` |
| 14 | Захват задач по URI | 4.2 | `tasks.uriCapture` |
| 15 | Обратные отсчёты и прогресс периода | 7.1 | `dashboard.countdowns`, `dashboard.periodProgress`, `dashboard.lifeWeeks` |
| 16 | Дашборд как стартовая страница | 7.2 | `dashboard.openOnStartup` |
| 17 | Итоги года | 8.2 | `journal.yearInReview` |

Сквозные работы: профили и экспорт/импорт — этап 1; API модулей v2 — этап 9; удаление
внешних API из контента — этап 2; новый источник времени намаза — этап 3.

Не вошли (решение автора): «В этот день», корреляции настроения, план против факта,
кибла, счётчик каза, план хатма, экспорт намаза в `.ics`, чтение ICS, следующая серия,
матрица Эйзенхауэра, виды Bases, модули финансов/тренировок/ИИ.

## Грубая оценка

| Этап | Объём |
|---|---|
| 0. Фундамент | 1,5–2 недели |
| 1. Профили | ~1 неделя |
| 2. Контент без API | 2–3 дня |
| 3. Время намаза | ~1 неделя |
| 4. Задачи и календарь | ~2 недели |
| 5. Дневник и привычки | ~2 недели |
| 6. Контент и намаз | ~1 неделя |
| 7. Дашборд | 4–5 дней |
| 8. Обзоры и итоги | ~1,5 недели |
| 9. API модулей v2 | ~2 недели |

---

## Решения автора (23 сентября 2026)

1. **Обложки контента** — оба варианта: файл из хранилища и вставленная ссылка.
   **Поля `externalRating` / `source` / `sourceId`** — поддержку убрать полностью (этап 2).
2. **Время намаза** расходилось с IslamApp. Причина — метод: IslamApp считает по MWL
   (18°/17°) со стандартным асром, Zenith по умолчанию — ДУМ РФ (16°/15°) с ханафитским.
   С MWL движок Zenith совпадает до минуты. Главный шаг этапа 3 — автоматический подбор
   «под моё приложение» и явный выбор метода при первом включении.
3. **Центр уведомлений** — своя история на каждом устройстве.
4. **«Жизнь в неделях»** — только вручную, ни в одном шаблоне.
5. **Ритуалы** — напоминания в 08:00 и 21:00, по умолчанию выключены.
6. **Шаблоны профилей** — набор из этапа 1 утверждён.
